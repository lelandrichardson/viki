/** @jsx React.DOM */
var React = require('react');
var TopNav = require('../components/TopNav');

var Viki = React.createClass({

	render: function() {
		return (
			<TopNav />
		);
	}

});

module.exports = Viki;